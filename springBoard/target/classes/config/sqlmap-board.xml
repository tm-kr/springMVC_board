<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="boardDAO">
	
	<!-- 페이지 조회 -->
	<select id="list" parameterType="int" resultType="boardVO">
		<![CDATA[
		  select * from (select rownum rnum, board.* from
		(select * from board order by NUM desc) board)
		where rnum >= 1 and rnum <=5
		 ]]>
	</select>
	
	<!-- 페이지 조회 페이징처리 -->
	<select id="listPage" parameterType="pageVO" resultType="boardVO">
		 <![CDATA[
		  select * from (select rownum rnum, board.* from
		(select * from board order by NUM desc) board)
		where rnum >= #{first} and rnum <= #{second}
		 ]]>
	</select>
	
	<select id="popularListPage" parameterType="pageVO" resultType="boardVO">
		 <![CDATA[
		  select * from (select rownum rnum, board.* from
		(select * from board order by views desc) board)
		where rnum >= #{first} and rnum <= #{second}
		 ]]>
	</select>
	
	<!-- 전체 글 갯수 가져오기 -->
	<select id="articleCount" resultType="int">
		select count(*) from BOARD
	</select>
	
	<!-- 게시물 보기 -->
	<select id="read" parameterType="int" resultType="boardVO">
		select * from BOARD
		WHERE num = #{num}
	</select>
	
	<!-- 게시물 작성 -->
	<insert id="write" parameterType="boardVO">
		insert into "BOARD" 
		(num, category, title, writer, regdate, views, content) 
		values
		(BOARD_SEQ.nextval, #{category}, #{title}, #{writer}, SYSDATE, 0, #{content})
	</insert>
	
	<!-- 조회수 올라가기 -->
	<update id="views" parameterType="int">
		UPDATE BOARD SET views = views + 1 WHERE num = #{num}
	</update>
	
</mapper>
